"""Core logic for the experiment.

Replace this module with the actual engineering problem being explored.
"""


def experiment(input_value: float) -> float:
    """Run the smallest possible deterministic experiment.

    This intentionally boring function gives the scaffold a real executable
    behavior and a place to start adding the project's actual logic.
    """
    return input_value * 2
